<Page
    x:Class="App1.Directories.MagicItemExtendedPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <local:RarityConvector x:Key="RarityConvert"/>
        <local:NameConvert x:Key="NameConvert"/>
    </Page.Resources>
    <ScrollViewer>
        <StackPanel x:Name="MainPanel">
            <TextBlock FontSize="24" Margin="30" Text="{Binding Name}"/>
            <StackPanel Margin="30,0,0,0" Orientation="Horizontal">
              
                <TextBlock Text="{Binding Quality, Converter= {StaticResource RarityConvert}}" Margin="0,0,10,0" />
                <TextBlock Text="under quality" Margin="0,0,15,0"/>
                <TextBlock Text="{Binding Type}" Margin="0,0,10,0" />
                <TextBlock Text="under type" />
            </StackPanel>
            <TextBlock Text="{Binding Description}" Margin="30,30,0,0" Loaded="ReFormateText"/>
            <TextBlock Text="Особенности" FontSize="20" Margin="30,30,0,15" TextDecorations="Underline"/>
            <ListView x:Name="FeaturesList" ItemsSource="{Binding Features, Mode=OneTime}" SelectionMode="None" Margin="30,0,0,30" >
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:MagicItemFeautes">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*" MinWidth="150"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock FontWeight="Black" Text="{Binding Name, Converter={StaticResource NameConvert}}" VerticalAlignment="Top">
                            </TextBlock>
                            <TextBlock Margin="10,0,0,0" Text="{x:Bind Desctipt}" Loaded="ReFormateText" VerticalAlignment="Top" Grid.Column="1" TextWrapping="Wrap">
                            </TextBlock>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="VerticalAlignment" Value="Stretch"/>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
            <Grid x:Name="TableGrid" Margin="30,0,0,0"/>
        </StackPanel>
    </ScrollViewer>
</Page>
